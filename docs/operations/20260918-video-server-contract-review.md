# 视频调用失败跨端核对

本轮核对的是2026-09-18用户提供的六条视频失败记录。当前桌面源码为1.1.75，
提交`81b2909b`，未因服务端故障修改或删除现有报价和预算字段。

Electron按当前协议提交`billing_quote_id`、`billing_version`、`client_version`和
`max_cost_micro_points`。服务端应在核验后将它们与上游执行参数分离。本次实际缺陷
在云端：清理后的JSON被解码回非空map，导致这些字段残留。修复位于云端
`apps/ai/rpc/internal/domain/relay/invocation/service.go`，不需要客户端另换一套请求。

桌面类型检查通过，标准测试300项通过、6项条件跳过；没有新的原生包构建或发布，
也不把源码检查说成用户设备完成实付生成。先前余额账户字段修复仍保留。

Fast记录中的参考图上传认证失败来自已受理任务的上游服务；云端修复其明确失败
分类与后续账务处理，但不删除参考图、不静默切换付费模型、不绕过预算确认。
两条更早的预占失败发生在个人账户收到分配之前，云端已补充正确钱包的错误说明。

完整根因、发布和历史复核处理状态以云端仓库
`docs/operations/20260918-video-failure-repair.zh-CN.md`及对应现场证据为准。

本轮服务端上线后，再读取当前12个模型和已有个人余额运行完整桌面标准测试，
301项通过、5项条件跳过。新增通过的现场项覆盖Electron正式投影到Renderer，
资产版本与4,100积分预占均保留，当前可消费995,900积分；没有新增付费生成。
该结果不代表Fast上游的参考图上传授权已经恢复，也不代表已在用户个人设备操作。
