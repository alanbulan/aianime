# H3 高清输出客户端适配记录

## 时间、范围与原因

- 日期：2026-09-14；桌面仓为 `ai-manga-desktop`，代码主仓 Gitee，发布镜像
  GitHub `alanbulan/aianime`。本次客户端版本 `1.1.70`。
- 服务端准备发布 H3 的精确 `1920x1080`、`1080x1920`、`1080x1080` 和
  `2520x1080`。客户端继续消费服务端目录，不硬编码供应商或新增调用协议。
- 同比例存在多个尺寸时，原比例切换逻辑总选目录中第一项，导致已选高清档退回低清。
  现优先匹配目标比例，再选择最接近当前短边像素的档位；默认尺寸和不支持档位的回退仍
  由目录决定。
- 远端精确竖屏五秒实测耗时约 23 分钟，长片超过旧 30 分钟窗口的风险已成立。
  视频生成轮询默认上限扩展到约六小时，`single_video` 和 `freezone_video_gen`
  的任务执行默认上限同步为六小时。显式轮询次数、环境超时覆盖和主动取消仍生效；
  合成、分析等其他任务继续原超时策略。

## 实施与验证

- 领域与参数面板测试验证四种尺寸可选、原值提交、比例切换保档及低清回退。
- Python 回归验证第 370 次查询才成功的任务只提交一次、不会在旧 360 次处取消，
  同时保留显式超时、取消失败提示与取消检查点。
- 修改的 Python 文件通过 Ruff；视频、任务超时及取消相关 49 项测试通过。
- 完整 Python 架构 212 项通过。首次发现发布 CLI 被误计为桌面运行时代码，
  修正扫描范围仅排除独立发布 CLI，仍要求桌面运行时唯一固定商业入口。
- 前端完整测试、类型检查和 CE 构建通过；桌面类型检查及 260 项测试通过，4 项
  平台专属测试按原规则跳过。版本一致性、锁文件检查和发布说明检查通过。
- 三平台安装包由既有 GitHub 工作流负责；构建完成不等于已安装和完成真机使用验收。

## 发布与兼容

- 正在构建的 `1.1.69` 不包含本次等待窗口与保档修复。
- `1.1.70` 使用同一三平台工作流排队构建，`cancel-in-progress: false`，不取消
  `1.1.69`。按用户要求不持续轮询构建。
- H3 长片用户应升级至 `1.1.70` 或更新版本，旧客户端仍有 30 分钟限制；只发布
  服务端尺寸目录不能修复旧客户端的等待逻辑。
- 代码提交 `d4d26c7b` 已同步到两个仓库，`workflow_dispatch` 返回 HTTP 204；
  一次回读确认运行号 `34818381550`、源码为该提交、状态为 `pending`，之后停止查询。
  执行链接：<https://github.com/alanbulan/aianime/actions/runs/34818381550>。
- 本记录不宣称当前安装包已构建、已上传服务器或已安装。

## 风险与回退

- 高清原生生成的耗时明显增加，六小时是默认等待上限，不是性能承诺；用户可随时取消。
- 回退客户端提交将恢复原比例选择和 30 分钟等待逻辑。服务端尺寸目录和 GPU 部署
  在云端仓独立记录，不能通过回退桌面代码撤销服务器能力。
