---
version: 1.1.20
attention: medium
---
# v1.1.20

## User-facing Highlights (zh)

- **来源提交**: 本版本由 Gitee Go 根据提交 `1a2a38b8be095445bedb86d29b83fef330f093e6` 自动生成，流水线构建编号为 `23`。
- **变更摘要**: fix(desktop): 修复快速导入进度卡死与重复调用 - 云端模型写请求遇到 502、503、504 时不再盲目重放，避免同一幂等调用被覆盖为 409 冲突 - 知识图谱流水线取消整套失败重跑，并限制 Cognee 文本与数据批次，降低并发调用对远端模型的压力 - 快速导入日志沿用当前任务进度并保证进度单调递增，修复状态长期停留在 0% 的假卡现象 - 补充模型读写重试边界、流水线单次执行、批次限制及任务进度回归测试
- **版本同步**: Python 包、Electron 安装器、前端版本兜底、依赖锁文件和 README 已统一更新为 1.1.20。
- **更新提示**: 更新窗口将展示本版本记录；安装完成后请重新启动客户端，使本地后端、Hermes 和 Electron 主进程全部切换到新版本。

## User-facing Highlights (en)

- **Source commit**: Generated by Gitee Go from commit `1a2a38b8be095445bedb86d29b83fef330f093e6` in pipeline build `23`.
- **Change summary**: fix(desktop): 修复快速导入进度卡死与重复调用 - 云端模型写请求遇到 502、503、504 时不再盲目重放，避免同一幂等调用被覆盖为 409 冲突 - 知识图谱流水线取消整套失败重跑，并限制 Cognee 文本与数据批次，降低并发调用对远端模型的压力 - 快速导入日志沿用当前任务进度并保证进度单调递增，修复状态长期停留在 0% 的假卡现象 - 补充模型读写重试边界、流水线单次执行、批次限制及任务进度回归测试
- **Version synchronization**: Python, Electron, frontend fallback, dependency lock, and README versions are synchronized to 1.1.20.
- **Update notice**: Restart the desktop client after installation so the local backend, Hermes, and Electron main process all use the new version.
