---
version: 1.1.12
attention: medium
---
# v1.1.12

## User-facing Highlights (zh)

- **来源提交**: 本版本由 Gitee Go 根据提交 `32d42230da2c6cc92dcfd6ff7eb18cd3e1f4b082` 自动生成，流水线构建编号为 `9`。
- **变更摘要**: fix(ci): 注入 Gitee Go 自动升版凭据 问题：仓库已经配置 AI_MANGA_PUSH_TOKEN 与 AI_MANGA_PUSH_USERNAME 全局变量，但流水线 YAML 未声明引用，导致质量门通过后自动版本阶段仍取不到密文并退出。 修复：按 Gitee Go 图形编辑器生成的正式格式，在 variables.global 中显式引用令牌和机器人用户名；补充流水线合同测试，防止后续编辑再次遗漏凭据绑定。 验证：流水线 YAML 可正常解析，版本管理与流水线合同测试 4 项全部通过，git diff --check 通过。
- **版本同步**: Python 包、Electron 安装器、前端版本兜底、依赖锁文件和 README 已统一更新为 1.1.12。
- **更新提示**: 更新窗口将展示本版本记录；安装完成后请重新启动客户端，使本地后端、Hermes 和 Electron 主进程全部切换到新版本。

## User-facing Highlights (en)

- **Source commit**: Generated by Gitee Go from commit `32d42230da2c6cc92dcfd6ff7eb18cd3e1f4b082` in pipeline build `9`.
- **Change summary**: fix(ci): 注入 Gitee Go 自动升版凭据 问题：仓库已经配置 AI_MANGA_PUSH_TOKEN 与 AI_MANGA_PUSH_USERNAME 全局变量，但流水线 YAML 未声明引用，导致质量门通过后自动版本阶段仍取不到密文并退出。 修复：按 Gitee Go 图形编辑器生成的正式格式，在 variables.global 中显式引用令牌和机器人用户名；补充流水线合同测试，防止后续编辑再次遗漏凭据绑定。 验证：流水线 YAML 可正常解析，版本管理与流水线合同测试 4 项全部通过，git diff --check 通过。
- **Version synchronization**: Python, Electron, frontend fallback, dependency lock, and README versions are synchronized to 1.1.12.
- **Update notice**: Restart the desktop client after installation so the local backend, Hermes, and Electron main process all use the new version.
