---
version: 1.1.18
attention: medium
---
# v1.1.18

## User-facing Highlights (zh)

- **来源提交**: 本版本由 Gitee Go 根据提交 `c02450ef441db0daf16ee419c7a862e8fea09d80` 自动生成，流水线构建编号为 `19`。
- **变更摘要**: test(frontend): 同步画布弹窗架构依赖门禁 失败原因：第 18 次 Gitee Go 构建的前端架构回归为 397 项通过、1 项失败。CanvasBrowserView 引入项目既有 AlertDialog 后，完整布局所有权测试中的固定依赖集合仍只允许 React、图标、国际化和视图模型，导致合法的共享 UI 依赖被误判。 修复内容：仅在 CanvasBrowserView 的精确依赖白名单中加入 AlertDialog 路径，不放宽模块公共 API、身份、共享 API 或控制器边界。 验证记录：失败断言按名称单独运行通过，1 项通过、337 项跳过；本机全量架构进程超时后已清理遗留测试进程，最终结果交由同配置的 Gitee Go 质量门复验。
- **版本同步**: Python 包、Electron 安装器、前端版本兜底、依赖锁文件和 README 已统一更新为 1.1.18。
- **更新提示**: 更新窗口将展示本版本记录；安装完成后请重新启动客户端，使本地后端、Hermes 和 Electron 主进程全部切换到新版本。

## User-facing Highlights (en)

- **Source commit**: Generated by Gitee Go from commit `c02450ef441db0daf16ee419c7a862e8fea09d80` in pipeline build `19`.
- **Change summary**: test(frontend): 同步画布弹窗架构依赖门禁 失败原因：第 18 次 Gitee Go 构建的前端架构回归为 397 项通过、1 项失败。CanvasBrowserView 引入项目既有 AlertDialog 后，完整布局所有权测试中的固定依赖集合仍只允许 React、图标、国际化和视图模型，导致合法的共享 UI 依赖被误判。 修复内容：仅在 CanvasBrowserView 的精确依赖白名单中加入 AlertDialog 路径，不放宽模块公共 API、身份、共享 API 或控制器边界。 验证记录：失败断言按名称单独运行通过，1 项通过、337 项跳过；本机全量架构进程超时后已清理遗留测试进程，最终结果交由同配置的 Gitee Go 质量门复验。
- **Version synchronization**: Python, Electron, frontend fallback, dependency lock, and README versions are synchronized to 1.1.18.
- **Update notice**: Restart the desktop client after installation so the local backend, Hermes, and Electron main process all use the new version.
