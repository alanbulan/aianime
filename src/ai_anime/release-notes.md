---
version: 1.1.21
attention: medium
---
# v1.1.21

## User-facing Highlights (zh)

- **来源提交**: 本版本由 Gitee Go 根据提交 `8d832e85a3169d7bfc6b498040c9af7fb29bd24f` 自动生成，流水线构建编号为 `26`。
- **变更摘要**: fix(desktop): 修复助手等待卡住与记住登录失效 - 项目聊天将客户端 turn_id 贯穿用户消息、Hermes/确定性回复和 SQLite 历史，重连后可精确识别已完成回合 - 前端在最终助手消息落库后立即结束等待，30 秒未收到完成帧时主动同步历史，并忽略迟到的重复完成帧 - 将记住登录凭据从会话令牌中拆分，继续使用 Electron safeStorage 加密；主动退出仅撤销会话，不删除租户、用户名和密码 - 登录页仅展示安全密码占位符，通过主进程复用加密密码，明文不进入渲染进程 - 补充后端、前端和 Electron 回归测试；类型检查、定向测试及正式前端构建通过
- **版本同步**: Python 包、Electron 安装器、前端版本兜底、依赖锁文件和 README 已统一更新为 1.1.21。
- **更新提示**: 更新窗口将展示本版本记录；安装完成后请重新启动客户端，使本地后端、Hermes 和 Electron 主进程全部切换到新版本。

## User-facing Highlights (en)

- **Source commit**: Generated by Gitee Go from commit `8d832e85a3169d7bfc6b498040c9af7fb29bd24f` in pipeline build `26`.
- **Change summary**: fix(desktop): 修复助手等待卡住与记住登录失效 - 项目聊天将客户端 turn_id 贯穿用户消息、Hermes/确定性回复和 SQLite 历史，重连后可精确识别已完成回合 - 前端在最终助手消息落库后立即结束等待，30 秒未收到完成帧时主动同步历史，并忽略迟到的重复完成帧 - 将记住登录凭据从会话令牌中拆分，继续使用 Electron safeStorage 加密；主动退出仅撤销会话，不删除租户、用户名和密码 - 登录页仅展示安全密码占位符，通过主进程复用加密密码，明文不进入渲染进程 - 补充后端、前端和 Electron 回归测试；类型检查、定向测试及正式前端构建通过
- **Version synchronization**: Python, Electron, frontend fallback, dependency lock, and README versions are synchronized to 1.1.21.
- **Update notice**: Restart the desktop client after installation so the local backend, Hermes, and Electron main process all use the new version.
