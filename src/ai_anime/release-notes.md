---
version: 1.1.11
attention: medium
---
# v1.1.11

## User-facing Highlights (zh)

- **来源提交**: 本版本由 Gitee Go 根据提交 `b9d66069c9c00b2ae0cd3dbce22cb0022b06a78e` 自动生成，流水线构建编号为 `1`。
- **变更摘要**: feat: 修复真实任务结果并接入 Gitee 自动版本流水线 助手任务：修复 Hermes 将成功查询结果中的历史 failed/error 字段误判为本次工具失败的问题；只对已知只读工具的 2xx 响应抑制领域历史失败，并将空响应替换为可操作的中文诊断提示。 视频结果：Electron 模型代理对视频内容接口校验 HTTP Content-Type，本地商业视频下载在落盘前继续校验 MP4 ftyp 文件头；云端返回 JSON、HTML、空文件或伪视频时明确失败并清理临时文件，避免把错误响应作为成片展示。 版本流水线：将 Gitee Go 默认 Maven/自有主机模板替换为 master push 自动触发的 Electron 版本流水线；新增统一版本脚本，每次源代码提交自动递增补丁版本，并同步 Python、Electron、uv.lock、Vite、README、云端交接文档和发布说明。 更新记录：流水线根据来源提交号、构建编号和完整提交说明生成中英文详细更新记录，再提交并推送 release 版本；release 提交通过提交前缀保护，不会再次递增，避免循环构建。 验证记录：Electron 66 项、Python 定向 35 项、架构边界 153 项测试全部通过；TypeScript 类型检查、Ruff、YAML 解析、版本一致性和 git diff 校验通过。
- **版本同步**: Python 包、Electron 安装器、前端版本兜底、依赖锁文件和 README 已统一更新为 1.1.11。
- **更新提示**: 更新窗口将展示本版本记录；安装完成后请重新启动客户端，使本地后端、Hermes 和 Electron 主进程全部切换到新版本。

## User-facing Highlights (en)

- **Source commit**: Generated by Gitee Go from commit `b9d66069c9c00b2ae0cd3dbce22cb0022b06a78e` in pipeline build `1`.
- **Change summary**: feat: 修复真实任务结果并接入 Gitee 自动版本流水线 助手任务：修复 Hermes 将成功查询结果中的历史 failed/error 字段误判为本次工具失败的问题；只对已知只读工具的 2xx 响应抑制领域历史失败，并将空响应替换为可操作的中文诊断提示。 视频结果：Electron 模型代理对视频内容接口校验 HTTP Content-Type，本地商业视频下载在落盘前继续校验 MP4 ftyp 文件头；云端返回 JSON、HTML、空文件或伪视频时明确失败并清理临时文件，避免把错误响应作为成片展示。 版本流水线：将 Gitee Go 默认 Maven/自有主机模板替换为 master push 自动触发的 Electron 版本流水线；新增统一版本脚本，每次源代码提交自动递增补丁版本，并同步 Python、Electron、uv.lock、Vite、README、云端交接文档和发布说明。 更新记录：流水线根据来源提交号、构建编号和完整提交说明生成中英文详细更新记录，再提交并推送 release 版本；release 提交通过提交前缀保护，不会再次递增，避免循环构建。 验证记录：Electron 66 项、Python 定向 35 项、架构边界 153 项测试全部通过；TypeScript 类型检查、Ruff、YAML 解析、版本一致性和 git diff 校验通过。
- **Version synchronization**: Python, Electron, frontend fallback, dependency lock, and README versions are synchronized to 1.1.11.
- **Update notice**: Restart the desktop client after installation so the local backend, Hermes, and Electron main process all use the new version.
